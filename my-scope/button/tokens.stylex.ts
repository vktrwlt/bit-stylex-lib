import * as stylex from '@stylexjs/stylex';

const tokensObj = {
  primaryColor: '#720b72',
  primaryColorLight: '#a93da9',
  secondaryColor: '#eeeaee',
};

export const tokens = stylex.defineVars(tokensObj);